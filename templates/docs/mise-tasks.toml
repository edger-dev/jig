# Shared docs tasks (from jig)

[tasks._docs-serve]
run = "cd docs && mdbook serve --port ${MDBOOK_DOCS_PORT:-8882} --hostname 0.0.0.0"
description = "Start mdbook live-reload dev server"

[tasks.docs-build]
run = "cd docs && mdbook build"
description = "Build mdbook static site"

# Task: rules installer v2

Build the downstream rule-installer automation into `/jig init` — the seam left
open by v1. A consuming rust project should get the lint-rule *kinos* into its
own kinora ledger with one idempotent, self-guarding command, while the
enforcement layer keeps installing without kinora.

## Realizes
- kino://4df64c1bac779eec6ac0806469abf38d31b92092eb4890319a03e96ac87b86ae — rule delivery (downstream install)

## Plan
kino://d463441440036eb2a224b44b302ab1f5f7739c38d1051e1161e3b22dd41bbf30

## Done when
- [x] `install-rules.sh` stores manifested rules into a target `rules` root,
      idempotently, with preconditions and a drift check
- [x] the installer stages only and prints the commit/next steps (no auto-commit)
- [x] `/jig init rust` and `/jig sync rust` invoke it (kinora-gated); the
      enforcement layer still installs without kinora
- [x] validated end-to-end against throwaway projects with the rebuilt kinora CLI

**Resolved.** Still open: the generated rung-0 hint digest, an `--update` mode
for re-versioning drifted downstream rules, and a dylint scaffold.

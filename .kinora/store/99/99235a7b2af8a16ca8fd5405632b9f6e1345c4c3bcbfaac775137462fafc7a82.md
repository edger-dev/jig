# Task: installer --update mode

Let a project refresh lint rules whose upstream source changed, without the
manual re-version dance — closing the "install skips existing" gap from
`jig::task::rules-installer-v2`.

## Realizes
- kino://4df64c1bac779eec6ac0806469abf38d31b92092eb4890319a03e96ac87b86ae — rule delivery (downstream install)

## Plan
kino://b6686b074a9eaa9673dfeba7064c3963f9d4190196865733f44955490aaccd91

## Done when
- [x] `install-rules.sh --update` re-versions only rules whose body changed,
      preserving identity and re-applying manifest metadata, with no `--root`
- [x] the plain (no-flag) install path is unchanged (still skips existing)
- [x] `kinora commit` after an update is clean (no ambiguous assigns)
- [x] the "changed but uncommitted" case is a soft `deferred` warning (exit 0)
- [x] reviewed by a fresh-eyes subagent; the one issue found was fixed

**Resolved.** Next: dylint scaffold.
